# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AXmacOSDemo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

   pod "Masonry"
  pod "AFNetworking"
  # WCDB支持iOS 7、macOS 10.9以上
  pod 'WCDB'
#  pod "NullSafe"
  pod 'SDWebImage'

end
